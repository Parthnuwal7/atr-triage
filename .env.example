# Production DB — READ-ONLY role, SELECT only. Never a writable user.
PROD_READ_DATABASE_URL=postgres://readonly_user:pass@prod-host:6432/atr
# Local store = embedded PGlite (NOT a postgres URL). A directory path persists data;
# 'memory://' is ephemeral. No Docker needed.
LOCAL_DATABASE_URL=./localdb
