services:
  triage-db:
    image: postgres:15
    environment:
      POSTGRES_USER: triage
      POSTGRES_PASSWORD: triage
      POSTGRES_DB: triage
    ports:
      - "5544:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
